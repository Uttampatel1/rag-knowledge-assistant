services:
  app:
    build: .
    image: rag-knowledge-assistant:latest
    container_name: rag-knowledge-assistant
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: unless-stopped
